<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yf.warehouse">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"	/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PhotoAbumActivity"/>
        <activity android:name=".activity.ShowImageActivity"/>
        <activity android:name=".activity.CinemaActivity"/>
        <activity android:name=".activity.ShowVideoActivity"/>
        <activity android:name=".activity.WidgetStyleActivity"/>
        <activity android:name=".activity.ListViewTitleGroupActivity"/>
        <activity android:name=".activity.WebPicShowActivity"/>
        <activity android:name=".activity.MapDemoActivity"/>
    </application>

</manifest>